to run the test

do the follwoing on the terminal  for face embeddings
source .venv/bin/activate
python face-embedding.py 


do the following for face detection
source .venv/bin/activate
python face_detect.py 


do the follwoing for gesture recognition
source .venv/bin/activate
python handg.py
